# AlphaEvolve Governance System Engine - Environment Variables Example
#
# Copy this file to .env and fill in your actual credentials and configurations.
# Do NOT commit the .env file to version control if it contains sensitive data.

# --- OpenAI LLM Service Configuration ---
# Your OpenAI API key, if you plan to use the OpenAILLMService.
# OPENAI_API_KEY="sk-your_openai_api_key_here"

# Default OpenAI model to use if not specified in code.
# Examples: "gpt-3.5-turbo", "gpt-4", "gpt-4-1106-preview" (for JSON mode)
# OPENAI_DEFAULT_MODEL="gpt-3.5-turbo"


# --- Cryptographic Service Configuration ---
# Paths to PEM-encoded private and public keys for the CryptoService, if pre-generated.
# These are optional; keys can be generated on the fly or loaded by other means.
# Ensure these files are kept secure if they are production keys.
# CRYPTO_PRIVATE_KEY_PATH="/path/to/your/private_key.pem"
# CRYPTO_PUBLIC_KEY_PATH="/path/to/your/public_key.pem"


# --- OPA (Open Policy Agent) Configuration ---
# Path to the OPA executable, if not in system PATH.
# OPA_EXECUTABLE_PATH="/usr/local/bin/opa"


# --- Logging Configuration ---
# Default log level for the application.
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
# APP_LOG_LEVEL="INFO"

# Path to a file for logging, if file logging is desired.
# APP_LOG_FILE="alphaevolve_app.log"


# --- Other Service-Specific Configurations (Add as needed) ---
# Example: Configuration for a hypothetical external audit service
# AUDIT_SERVICE_API_URL="https://api.example.com/audit"
# AUDIT_SERVICE_API_KEY="your_audit_service_key"

# Note: Lines starting with # are comments and are ignored.
# Remove the # from variables you wish to set and provide appropriate values.
#
# This .env.example file is meant to be a template. You may not need all settings,
# or you might need others depending on the services and features you use.
# Always refer to the documentation for specific services (e.g., LLM providers)
# for the most up-to-date environment variable names and formats.
