# Database URL for the Artificial Constitution (AC) service
# Example for PostgreSQL: postgresql://username:password@host:port/database_name
DATABASE_URL="postgresql://youruser:yourpassword@postgres:5432/yourdatabase_ac"

# Optional: Set to "development" or "production"
# APP_ENV="development"

# Placeholder for auth token if needed for direct service-to-service calls (not used yet by AC service directly)
# AUTH_SERVICE_TOKEN="some_internal_token"
